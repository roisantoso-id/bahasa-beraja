services:
  nginx:
    image: nginx:alpine
    container_name: bahasa-beraja-nginx-proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx-proxy.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - app_network

networks:
  app_network:
    external: true
    name: bahasa_app_network 